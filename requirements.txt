# KOS Engine V5.1 — Dependencies
# Install: pip install -r requirements.txt

# Core
nltk>=3.8.0
jellyfish>=1.0.0
sympy>=1.12
numpy>=1.24.0

# Web foraging
beautifulsoup4>=4.12.0
requests>=2.31.0

# Graph visualization
networkx>=3.0
pyvis>=0.3.0

# Web UI
streamlit>=1.30.0

# Optional: LLM mode (not required for offline)
openai>=1.0.0

# NOTE: These are optional heavy dependencies.
# Uncomment locally if needed. Railway free tier cannot handle them.
# sentence-transformers>=2.2.0   # Layer 5 semantic fallback (~2GB with torch)
# faiss-cpu>=1.7.0               # Scaling to 100K+ nodes
# z3-solver>=4.12.0              # Formal verification
